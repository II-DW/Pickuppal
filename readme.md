# Pickup Pal (픽업팰) 🌍🏃‍♂️🎁

**픽업팰(Pickup Pal)**은 음식 배달 대신 **포장(픽업)**을 유도하여 건강을 챙기고, 탄소 배출을 줄이며, 배달비를 절약하는 **게이미피케이션(Gamification) 기반의 친환경 주문 서비스 앱**입니다.

사용자는 걷기를 통해 로봇 아바타를 성장시키고, 획득한 포인트로 혜택을 누리며, 친구들과 랭킹 경쟁을 즐길 수 있습니다.

---

## ✨ 주요 기능 (Key Features)

### 1. 🛵 배달 및 🏃 픽업 주문
* **다양한 카테고리:** 치킨, 피자, 분식, 카페 등 다양한 메뉴를 탐색할 수 있습니다.
* **스마트 픽업:** 픽업 주문 시 **배달비 0원** 혜택은 물론, 이동 거리에 따른 **칼로리 소모량**과 **탄소 절감량**을 계산해 보여줍니다.
* **다회용기 옵션:** 일회용품 줄이기를 위해 '다회용기 사용'을 선택하면 추가 보너스 포인트를 지급합니다.

### 2. 🎮 RPG 요소 & 아바타 성장
* **3D 아바타 ('피키'):** 픽업 활동으로 경험치(EXP)를 얻어 아바타 레벨을 올릴 수 있습니다. (Google `model-viewer` 기반 3D 애니메이션 적용)
* **스탯 시스템:** 레벨업 시 스탯 포인트를 획득하여 캐릭터를 강화할 수 있습니다.
    * **⚔️ 공격력:** 포인트 적립률 증가 (더 많은 포인트 획득)
    * **🛡️ 방어력:** 배달비 할인 (배달 주문 시 비용 절감)

### 3. 💰 포인트 시스템 & 상점
* **이원화된 포인트:**
    * **누적 포인트 (XP):** 랭킹 산정 및 레벨업 기준 (줄어들지 않음).
    * **사용 가능 포인트 (Cash):** 실제 현금처럼 사용하는 재화.
* **포인트 획득:** 픽업 주문, 걷기(만보기 연동), 다회용기 사용 등을 통해 획득.
* **포인트 상점 (룰렛):** 포인트를 사용하여 할인 쿠폰이나 대박 포인트를 뽑을 수 있는 **행운의 룰렛** 기능 제공.
* **결제 사용:** 주문 결제 시 포인트를 사용하여 즉시 할인 가능.

### 4. 🗺️ 실시간 경로 안내 (시뮬레이션)
* **경로 맵:** 픽업 주문 시 매장까지의 이동 경로를 지도상에 시각화하여 보여줍니다.
* **도착 알림:** 매장에 도착하면 알림 모달과 함께 **폭죽 애니메이션**으로 보상 획득을 축하해줍니다.

### 5. 🏆 랭킹 & 커뮤니티
* **실시간 랭킹:** 지역, 친구, 주간별로 누적 포인트 순위를 매겨 경쟁심을 유발합니다.
* **보상:** 상위 랭커(1~3위)에게는 특별한 메달 배지가 부여됩니다.

---

## 🛠️ 기술 스택 (Tech Stack)

* **Platform:** iOS
* **Language:** Swift
* **Framework:** SwiftUI
* **Architecture:** MVVM (Model-View-ViewModel)
* **3D Rendering:** `WKWebView` + `<model-viewer>` (GLB 파일 렌더링 및 애니메이션 제어)
* **External Library:** `ConfettiSwiftUI` (주문 완료 축하 효과)
* **Concurrency:** Swift Concurrency (`async`/`await`)

---

## 📂 프로젝트 구조 (Project Structure)

PickupPal
├── Models/                 # 데이터 모델 (User, Restaurant, Activity 등) <br />
│   └── DataTypes.swift<br />
├── ViewModels/             # 비즈니스 로직 및 상태 관리<br />
│   └── AppViewModel.swift<br />
├── Views/                  # UI 컴포넌트<br />
│   ├── ContentView.swift       # 메인 진입점 및 탭 관리<br />
│   ├── Home/                   # 홈 탭 관련 뷰 (통계 카드, 랭킹 등)<br />
│   ├── Delivery/               # 배달/픽업 주문 관련 뷰 (메뉴, 지도 등)<br />
│   ├── Components/             # 재사용 가능한 UI (버튼, 로딩, 헤더 등)<br />
│   ├── Modals/                 # 팝업 뷰 (로그 입력, 아바타 관리 등)<br />
│   └── Shop/                   # 포인트 상점 (룰렛)<br />
├── Services/               # API 통신 및 데이터 처리 (Mock Data)<br />
│   └── APIManager.swift<br />
└── Utilities/              # 확장 기능 및 헬퍼<br />
    └── Extensions.swift<br />

## 🚀 설치 및 실행 (How to Run)

1.  **요구 사항:**
    * Mac (macOS 최신 버전 권장)
    * Xcode 14.0 이상
    * iOS 16.0 이상 타겟

2.  **라이브러리 설치:**
    * 이 프로젝트는 **Swift Package Manager (SPM)**를 사용합니다.
    * 프로젝트를 열면 자동으로 패키지를 가져오지만, 만약 실패한다면 `File > Add Packages...`에서 다음 URL을 추가하세요.
        * `https://github.com/simibac/ConfettiSwiftUI.git`

3.  **권한 설정 (Info.plist):**
    * 실제 기기에서 걸음 수를 측정하려면 `Privacy - Motion Usage Description` 권한이 필요합니다. (현재 데모 버전은 랜덤 값으로 시뮬레이션됩니다.)

4.  **빌드 및 실행:**
    * `Cmd + R`을 눌러 시뮬레이터 또는 연결된 아이폰에서 실행합니다.

## 📝 추후 개선 사항 (To-Do)

* [ ] **CoreMotion 연동:** 실제 사용자의 걸음 수 데이터를 HealthKit과 연동하여 정밀 측정.
* [ ] **지도 API 연동:** MapKit 또는 Google Maps를 연동하여 실제 위치 기반 경로 안내 구현.
* [ ] **백엔드 서버 구축:** Mock 데이터를 실제 DB(Firebase 또는 REST API)로 교체하여 멀티 유저 환경 지원.
* [ ] **3D 아이템 커스터마이징:** SceneKit을 도입하여 아바타에 모자, 옷 등 아이템 착용 기능 추가.
* [ ] **아바타 경쟁 시스템:** 아바타끼리 경쟁을 통하여 사용자들끼리 경쟁할 수 있는 시스템 구축.
* [ ] **HealthKit 연동:** 건강앱과 연동을 통해 걸음 수 추적을 쉽게하기.
